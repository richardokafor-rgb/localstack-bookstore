{
  "_comment": "MCP_API_ENDPOINT changes after every LocalStack restart. Run `make -C infrastructure output` to get the current api_endpoint, then update this file, or run `./infrastructure/scripts/update-mcp-config.sh`.",
  "mcpServers": {
    "bookstore": {
      "command": "python3",
      "args": [
        "services/mcp-skill/server.py"
      ],
      "env": {
        "MCP_API_ENDPOINT": "http://4bcff56d.execute-api.localhost.localstack.cloud:4566",
        "ORDER_SERVICE_URL": "http://localhost:5001",
        "AWS_ENDPOINT_URL": "http://localhost:4566",
        "AWS_DEFAULT_REGION": "us-east-1",
        "AWS_ACCESS_KEY_ID": "test",
        "AWS_SECRET_ACCESS_KEY": "test"
      }
    }
  }
}
